# Deps for the online BitNet trainer worker (scripts/train_online.py) ONLY.
# Deliberately separate from the API's requirements.txt so the FastAPI image
# stays lean — these are heavy and only the trainer worker needs them.
#
# CPU is enough for BitNet (1-bit base + small LoRA). Install the CPU torch build:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-trainer.txt
torch>=2.2            # install the CPU wheel via the index-url above
transformers>=4.44
peft>=0.11
datasets>=2.19
accelerate>=0.30
requests>=2.31
